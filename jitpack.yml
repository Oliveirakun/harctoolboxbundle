jdk:
  - oraclejdk8
before_install:
   - tools/build-DevSlashLirc.sh
   - tools/build-dependencies.sh
install:
   - echo "Installing only HarcHardware"
   - mvn clean install -pl HarcHardware -am -DskipTests 
